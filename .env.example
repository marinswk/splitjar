# Lock iframe embedding down to specific origins via a frame-ancestors CSP.
# Leave unset to allow embedding from anywhere (recommended for trusted
# local networks behind a reverse proxy). Set this only if you want to
# restrict who can embed splitjar.
# SPLITJAR_FRAME_ANCESTORS='self' https://your-home-assistant.example.com
