services:
  splitjar:
    build: .
    image: ghcr.io/marinswk/splitjar:latest
    container_name: splitjar
    restart: unless-stopped
    ports:
      - "8473:8473"
    volumes:
      - splitjar_data:/data
    env_file:
      - path: .env
        required: false
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8473/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  splitjar_data:
